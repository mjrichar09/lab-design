# Human Test — feedback ledger

> **Placeholder / stub.** The capture inbox for feedback from anyone who tries
> the layout tool (operators, EHS, facilities, process/ops). Log items here the
> moment they arrive — don't wait. Update the status column as items resolve.

How to use: one row per distinct piece of feedback. `id` is sequential
(`H1`, `H2`, …). `status` is `open` / `in-progress` / `resolved` / `wontfix`.
When resolved, note where it landed (commit, layout version, or TODO item).

| id | date | source | feedback | status | resolution |
|---|---|---|---|---|---|
| _example_ | 2026-07-10 | — | (e.g. "resize handle too easy to grab while dragging") | resolved | Lock-sizes toggle added (§5) |

_Add new rows below. Nothing captured yet from a real testing session._
